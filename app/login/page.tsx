import { AuthForm } from "@/components/auth/auth-form";

export default function LoginPage() {
  return <main className="cyber-shell flex min-h-screen items-center justify-center p-4"><AuthForm mode="login" /></main>;
}
